/** Generic sign-in copy. Never reveals whether an email address is known. */
export const CODE_SENT_MESSAGE = 'If that address is known to us, a code is on its way.'
export const CODE_INVALID_MESSAGE = 'That code is invalid or expired.'
